spring:
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  datasource:
    url: jdbc:postgresql://localhost:5432/testtms
    username: postgres
    password: 148148
    driver-class-name: org.postgresql.Driver
---
spring:
  config:
    activate:
      on-profile: test
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:h2:file:./db/tmsfm;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: test
    password: test
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true

server:
  port: 8080
  error:
    include-message: always

logging:
  level:
    org:
      zolando:
        logbook: debug
    root: debug

